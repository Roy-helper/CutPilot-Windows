# CutPilot dependencies

# AI
openai>=1.0
httpx>=0.24

# Config
pydantic>=2.0
pydantic-settings>=2.0

# Video editing
moviepy>=2.0
pillow>=10.0

# Desktop UI
pywebview>=5.0
bottle>=0.12
certifi

# Speech recognition (lightweight, no torch)
faster-whisper>=1.0
certifi

# Optional: FunASR (best Chinese, hotwords + speaker diarization)
# pip install funasr torch torchaudio

# Hardware detection (no extra deps needed)
